# Nixpacks configuration for Coolify deployment

[variables]
NIXPACKS_NODE_VERSION = '22'

[phases.setup]
nixPkgs = ['nodejs_22']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'node ./dist/server/entry.mjs'
