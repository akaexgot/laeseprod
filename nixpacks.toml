# Nixpacks configuration for Coolify deployment

[phases.setup]
nixPkgs = ['nodejs_20', 'npm']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'node ./dist/server/entry.mjs'
